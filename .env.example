# Railway Production Environment Variables
# These will be set in Railway's environment variables section

# MongoDB Configuration - Set in Railway
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/university_bot?retryWrites=true&w=majority
DB_NAME=university_bot
USERS_COLLECTION=users
FAQS_COLLECTION=faqs

# JWT Configuration - Set a strong secret in Railway
JWT_SECRET_KEY=your-super-secure-jwt-secret-key-here

# API Configuration
API_HOST=0.0.0.0
# PORT is automatically set by Railway

# Production Settings
DEBUG=false
ENVIRONMENT=production
